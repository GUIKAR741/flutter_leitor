class AnimeModel {
  AnimeModel();

  factory AnimeModel.fromJson(Map<String, dynamic> json) {
    return AnimeModel(
        //field: json[''],
        );
  }

  Map<String, dynamic> toJson(AnimeModel model) {
    Map<String, dynamic> json = Map<String, dynamic>();

    return json;
  }
}
