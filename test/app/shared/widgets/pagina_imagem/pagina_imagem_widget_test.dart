import 'package:flutter_test/flutter_test.dart';
// import 'package:flutter_modular/flutter_modular_test.dart';

// import 'package:flutter_leitor/app/shared/widgets/pagina_imagem/pagina_imagem_widget.dart';

main() {
  testWidgets('PaginaImagemWidget has message', (WidgetTester tester) async {
    // await tester.pumpWidget(buildTestableWidget(PaginaImagemWidget()));
    // final textFinder = find.text('PaginaImagem');
    // expect(textFinder, findsOneWidget);
  });
}
