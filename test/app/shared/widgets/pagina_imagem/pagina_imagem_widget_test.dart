// import 'package:flutter_leitor/app/shared/widgets/pagina_imagem/pagina_imagem_widget.dart';
import 'package:flutter_test/flutter_test.dart';
// import 'package:flutter_modular/flutter_modular_test.dart';

main() {
  testWidgets('PaginaHqWidget has message', (WidgetTester tester) async {
    // await tester.pumpWidget(buildTestableWidget(PaginaImagemWidget(url: 'http://lajsd')));
    // await tester.pump(Duration(minutes: 1));
    // final textFinder = find.text('Erro ao carregar');
    // expect(textFinder, findsOneWidget);
  });
}
